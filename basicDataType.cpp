#include <iostream>
#include <cstdio>
using namespace std;

int main() {
    int it;
    long lng;
    char ch;
    float fot;
    double dob;
    scanf("%d %ld %c %f %lf", &it, &lng, &ch, &fot, &dob);
    printf("%d\n%ld\n%c\n%f\n%lf", it, lng, ch, fot, dob);
    return 0;
}
